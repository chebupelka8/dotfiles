```python
# после * в параметрах функции можно использовать любой порядок параметров со значениями по умолчанию и без. после * python указывает на то что все следующие параметры являются kwargs (тоесть должны указываться по именам)

def func(*, arg1: str = "hello, world", arg2: int):
	...

func(arg1="adsfadf", arg2=10)

# !!!
func("asdfas", 1) # так не сработает
```